#!/usr/bin/env bash
set -euo pipefail

FAM="${1:?uso: gh-train-family 'nome famiglia'}"

cd /opt/greenbrain-platform/apps/ml-worker
source /opt/greenbrain-platform/apps/ml-worker/.venv/bin/activate
set +H
set -a
set +a

RUN_TYPE=train_family_manual \
RUN_TRIGGER_SOURCE=manual \
/opt/greenbrain-platform/apps/ml-worker/jobs/train_one_safe.sh "$FAM"
