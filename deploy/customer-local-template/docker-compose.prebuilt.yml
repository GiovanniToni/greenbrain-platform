services:
  backend:
    image: ${GREENBRAIN_BACKEND_IMAGE:-ghcr.io/giovannitoni/greenbrain-customer-backend:${GREENBRAIN_IMAGE_TAG:-latest}}
    build: null

  ml-worker:
    image: ${GREENBRAIN_ML_WORKER_IMAGE:-ghcr.io/giovannitoni/greenbrain-customer-ml-worker:${GREENBRAIN_IMAGE_TAG:-latest}}
    build: null
