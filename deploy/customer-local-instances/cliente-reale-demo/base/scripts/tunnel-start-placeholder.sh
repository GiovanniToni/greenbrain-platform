#!/usr/bin/env bash
set -euo pipefail

BASE_DIR="$(cd "$(dirname "$0")/.." && pwd)"

echo "Placeholder: qui andrà l'avvio del tunnel outbound sicuro."
echo "Directory instance: $BASE_DIR"
echo "Config attesa: $BASE_DIR/tunnel/cloudflared/config.yml"
echo "Nessuna attivazione reale eseguita."
