tunnel: REPLACE_WITH_REAL_TUNNEL_UUID
credentials-file: /etc/cloudflared/REPLACE_WITH_REAL_TUNNEL_UUID.json

ingress:
  - hostname: cliente-reale-demo-4.greenbrain.it
    service: http://host.docker.internal:8088
  - service: http_status:404
