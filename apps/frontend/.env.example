# GreenBrain Frontend — environment variables
# Copy to .env and fill in values. Never commit .env to version control.

# Backend API — the only required variable.
# The frontend uses JWT auth via the backend; Supabase SDK is not used.
VITE_API_BASE_URL=http://localhost:8000
